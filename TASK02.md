# TASK-02 — 하네스 결정성 확보

작성: 코워커 · 2026-07-29
선행: `CLAUDE.md`, `HARNESS.md`, `DET-REPORT.md`
**게임 코드(`index.html`)는 한 줄도 건드리지 않는다. 측정 도구만 고친다.**

> **번호에 대하여** — 오퍼스는 이 지시서를 `TASK-01.md`로 덮어쓰라고 했으나 그렇게 하지 않았다.
> `TASK-01.md`(함선 필터)는 **이미 완료된 작업의 이력**이다. 덮어쓰면 무엇을 왜 했는지가 사라진다.
> 이 프로젝트는 오염된 이력 때문에 이미 한 번 데이터를 전량 폐기했다. 번호는 재사용하지 않는다.
> 함선 필터는 `TASK-01.md`로 남고, 로드맵 순서상 4번으로 이동한다.

---

## 1. 문제 — 세 하네스 전부 신뢰할 수 없다

| 하네스 | 결정성 | 사각지대 |
|---|---|---|
| 오퍼스 (node) | ✅ 확보 | **타이머 로직이 전혀 안 보임** — `setTimeout`을 `()=>0`으로 스텁 |
| 코워커 (브라우저) | ❌ 미확보 | 원인 3종 (아래) |
| 코드 (node, 저장소) | **미확인** | 미확인 |

**둘은 대체재가 아니라 보완재다.** 오퍼스 것은 결정적이지만 타이머를 못 보고, 코워커 것은 타이머를 보지만 결정적이지 않다.
**이번 작업의 목표는 "결정적이면서 타이머도 보이는" 하나의 방식을 양쪽에 이식하는 것이다.**

### 코워커 하네스의 비결정 원인 3종 (실측 확인)

동일 시드(4242), 동일 화면, **실행 리듬만 변경** 3회 → AI 시대 3/2/2, 골드 847/304/194, 병력 50/42/49.

**① 사운드가 시뮬 난수를 훔친다 — 전체의 98%**

`noiseS()`가 `Math.random()`을 소비하고, 그 호출이 `rl(k,ms)`의 `performance.now()` 게이팅을 받는다.
1인전 시뮬도 `Math.random()`을 쓰므로(`SR()`은 2인전 전용) 하네스가 이를 시드 난수로 덮으면 **사운드가 시뮬의 난수를 가져간다.**
**헤드리스 크로미움에도 `AudioContext`가 존재한다** — `[object AudioContext]`로 확인했다. node 스텁 하네스만 `!AC`로 자동 회피된다.

**② 게임의 rAF 루프가 수동 스텝과 동시에 돈다**

`loop()`이 `requestAnimationFrame`으로 돌면서 `running`인 동안 `update(dt)`를 호출한다.
하네스가 수동으로 `update()`를 부르는 동안 **게임이 벽시계 속도로 나란히 시뮬을 진행시킨다.**
"300초 돌렸다"고 기록한 판이 실제로는 300초 + 미지의 양이었다.

**③ `setTimeout` 콜백이 서로 다른 시뮬 시각에 끼어든다 — 이번 작업의 본체**

①②를 막아도 어긋난다(4,496 / 4,515 / 4,541). 하네스가 타이머 숨통을 틔우려 양보하는 리듬이 달라지면
대기 중인 콜백이 다른 시뮬 시각에 실행된다. 분산은 작지만(±0.5%) 시뮬레이션이 혼돈계라 결과가 갈린다.

---

## 2. 해법 — 가상 타이머 큐

**벽시계를 시뮬 시각으로 대체하고, 만기된 콜백만 `update()` 직후에 순서대로 실행한다.**

### 명세

하네스가 시뮬 시각 `T`(초)를 소유한다. 매 스텝 `T += dt`.

**`setTimeout(fn, ms)` 대체**
- 큐에 `{ due: T + ms/1000, seq: n++, fn }` 삽입, 정수 id 반환
- `ms` 생략 시 0으로 취급

**`clearTimeout(id)` / `clearInterval(id)` 대체**
- 큐에서 해당 id 제거

**`setInterval(fn, ms)` 대체**
- 동일하되 실행 후 `due += ms/1000`으로 재삽입

**실행 시점** — 매 `update(dt)` **직후**
- `due <= T`인 항목을 `due` 오름차순, 동률이면 `seq` 오름차순으로 실행
- **콜백 실행 중 새로 등록된 타이머는 이번 라운드에서 실행하지 않는다** (다음 스텝으로)
- 이 규칙이 없으면 연쇄 등록이 무한 루프가 된다

**`performance.now()` / `Date.now()` 대체**
- `T * 1000` 반환. `rl()`의 게이팅이 시뮬 시각 기준이 되어 결정론화된다

**`requestAnimationFrame` 대체**
- 무력화(`()=>0`). 하네스가 `update()`를 직접 부르므로 이중 스텝을 막는다
- 렌더 거동을 봐야 하면 같은 큐에 16.7ms 간격으로 넣되, **A/B 측정 시에는 반드시 무력화**

### 사운드는 별도로 봉인한다 — `muted = true`

**가상 타이머만으로는 부족하다.** `rl()`이 결정론화돼도 `noiseS`의 난수 소비량이

```js
const len = Math.floor(AC.sampleRate * dur);
for(let i=0;i<len;i++) d[i] = (Math.random()*2-1)*(1-i/len);
```

**`AC.sampleRate`에 비례한다.** 44,100Hz 기기와 48,000Hz 기기에서 소비량이 달라진다.
같은 시드가 **기기에 따라 다른 결과**를 내게 된다.

→ **`muted = true`를 게임 시작 직전에 설정한다.** `noiseS`·`tone` 모두 첫 줄에서 반환하고 난수를 소비하지 않는다.
사운드는 게임 로직에 영향이 없으므로 잃는 것이 없다.

---

## 3. 얻는 것 — 사각지대가 사라진다

가상 타이머 큐를 넣으면 **오퍼스 하네스가 지금 못 보는 것들이 보이기 시작한다.**

| 위치 | 내용 | 현재 |
|---|---|---|
| 2004줄 | `useTactic`의 **폭격 요청** — 6회 폭격이 전부 `setTimeout` | 검증 불가 |
| 3411줄 | `beginFall` 함락 연출 — 폭발 4회 + 최종 폭발 | 검증 불가 |
| 1662 / 2096줄 | `aiNukeWin` / `launchNuke` | 검증 불가 (현재 죽은 코드) |

**로드맵 2번(전술 재연결)에서 전술 5종 중 "폭격 요청"을 재려면 이 작업이 선행되어야 한다.**

---

## 4. 완료 조건 — 이것을 통과해야 완료다

**세 하네스 각각에 대해:**

> 동일 시드로, **실행 리듬을 일부러 바꿔가며** 3회 실행하여 전 지표가 완전 일치할 것.

리듬 변경의 예 — 매 회차마다 양보 간격과 양보 시간을 다르게 준다:

```
1회차: 200스텝마다 3ms 양보
2회차: 337스텝마다 6ms 양보
3회차: 474스텝마다 9ms 양보
```

**리듬을 고정한 채 3회 돌려 일치하는 것으로는 통과가 아니다.** 우연히 맞은 것이다.
내가 처음에 그렇게 넘어갔고, 그래서 이 세션의 실측을 전부 폐기했다.

비교 지표는 최소 이것들 — 판 길이, 양측 킬, 양측 시대, 양측 골드, 양측 지휘부 HP, 병력 수, `Math.random` 누적 호출 수.
**`Math.random` 호출 수를 반드시 포함하라.** 결과가 우연히 같아도 호출 수가 다르면 결정성이 없다.

---

## 5. 작업 범위

**한다**
- 저장소의 하네스 4종(`harness23`·`harness27_2p`·`harness30`·`diag`)에 가상 타이머 큐 적용
- 공통 모듈로 분리 — 네 곳에 같은 코드를 복사하지 마라
- `muted = true`, `requestAnimationFrame` 무력화 적용
- 완료 조건 검사를 스크립트로 만들어 저장소에 넣는다 (`det-check.js` 등)
- `HARNESS.md`에 새 방식과 검사 절차를 기재

**하지 않는다**
- **`index.html`을 건드리지 않는다.** 이번 작업에 게임 코드 변경은 없다
- 밸런스 수치를 바꾸지 않는다
- 이전 A/B 결과를 다시 돌리지 않는다 — 도구가 준비된 뒤에 별도 지시로 한다

---

## 6. 보고

- **한국어로.** 사장은 코드를 읽지 못한다
- 완료 조건 검사 결과를 **하네스별로, 3회 수치 전부** 제시하라. "일치함"만으로는 부족하다
- 통과하지 못한 하네스가 있으면 **통과한 것만 통과라고 보고하고 나머지는 원인과 함께 남겨라.** 부분 통과를 완료로 부르지 마라
- 변경 목록 전량. 보고가 여러 번에 걸치면 마지막에 하나로 합쳐 다시 제출
- 각 블록 말미에 `[코드 → 사장]` 형식으로 대상을 표기

## 7. 산출

수정된 하네스 파일들과 `HARNESS.md`를 **첨부로** 낸다. 푸시를 시도하지 마라.
`index.html`은 이번 작업의 산출물이 아니다.

---

## 8. 왜 이걸 먼저 하는가 — 한 문장

**오퍼스와 코워커가 각자 자기 측정이 틀렸다는 것을 오늘 발견했다.**
며칠 전이었으면 그 숫자로 밸런스를 정했을 것이다. 도구를 고치기 전의 어떤 숫자도 근거가 되지 못한다.
