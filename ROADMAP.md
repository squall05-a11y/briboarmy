# ARMY EVOLUTION — 로드맵

이 문서는 확정된 작업 순서와 설계 결정을 기록한다.
새 세션은 `CLAUDE.md`(협업 규칙)와 이 문서를 먼저 읽는다.

---

## 확정된 설계 결정

**주 플랫폼은 PC다.** 넓은 전장, 휠 줌, 압도적 물량, 긴 판, 미래 시대까지 가는 문명 레이스 — 전부 PC 감각이다. 폰은 같은 코드의 세로 모드로 남는다.

**코드는 한 벌이다.** 프로젝트를 둘로 나누지 않는다. 시뮬레이션은 세로축(내가 아래, 적이 위)을 유지하고, PC에서는 그 월드를 눕혀서 그린다. 월드가 고정되므로 화면이 달라도 밸런스가 동일하다.

**"나중에 폰으로 간추린다"에 의존하지 않는다.** 기능을 추가할 때마다 PC 전용인지 공용인지 그 자리에서 정한다. 나중에 잘라내기는 실제로 거의 일어나지 않는다.

**RTS식 개별 조작은 넣지 않는다.** 병력이 1000을 넘고 승급 시스템상 개별 병사가 조작 단위로 존재하지 않는다. 압도적 물량과 개별 조작은 양립할 수 없다.

**작전·대형은 클릭 하나로 전군에 적용한다.** 부대 선택도 드래그도 없다. PC의 넓은 화면은 대형을 여러 개 동시에 노출하는 데 쓴다(폰은 스크롤, PC는 나란히 — 같은 기능, 다른 배치).

**적 수도는 화면 밖에 둔다.** 카메라 축소 한계는 완화하되 제거하지 않는다. 원정을 가야 적진을 본다.

**시간 강제 종료 장치는 부활시키지 않는다.** 짧게 끝나도 좋고 40분이 되어도 좋다. 교착은 시계가 아니라 실제 정체 상태로 감지한다. 안전망은 자율 AI가 먼저 작동한 뒤 최후 수단으로만 검토한다.

---

## 작업 순서

### 1. 인구 카드 함선 유입 필터 (승인됨 · 첫 작업)
조선소 없이 함선이 새어 들어오는 경로가 셋이다.
- `triadCards` 인구 풀 — `!u.naval` 없음
- `applyItem` 보급 상자 🎁 풀 — `!u.naval` 없음
- `onEraUp` **AI 시대 보상 풀** — `!u.naval` 없음. 같은 함수 안에서 플레이어 보상 풀은 함선을 제외한다. **AI만 공짜 함선을 받는 비대칭**
- `slotEval`에도 같은 누락이 있으나 `openSlot`이 호출되지 않는 죽은 코드로 보임 — 확인 후 판단

수정 후 A/B 재실측. 이 한 바퀴로 워크플로 감을 잡는다.

### 2. 전술 시스템 재연결
`TACTICS`에 시대별 5종(일제사격·화살 폭풍·전군 돌격·폭격 요청·탄막 집중)이 완성돼 있으나 **`useTactic()`을 호출하는 곳이 코드 어디에도 없다.** 전술 버튼은 시대와 무관하게 동일한 버프(공속 +40%, 피해 -25%)만 건다.

부작용: 시대가 오를 때마다 "⚔ 필살기 진화 — {이름}" 토스트가 뜨지만 실제로는 바뀌지 않는다. **게임이 거짓 정보를 표시하고 있다.**

`DOCTRINES`도 항목이 하나뿐이라 개국 독트린 선택이 사실상 무선택이다.

값이 싸고 효과가 즉시 보인다. 후반부 공백을 일부 메운다.

### 3. 작전·대형 시스템
`layoutArmy()`가 유일한 배치 규칙(중앙 밀집, 52px 간격)을 갖고 있다. 여기에 대형별 규칙을 얹는다.

가위바위보가 성립해야 한다:
- **밀집 방진** — 근접 강함 / 포격에 취약
- **산개 횡대** — 포격 피해 급감 / 뚫리면 각개격파
- **쐐기 돌격** — 전진 빠름 / 포병 노출
- **종심 방어** — 느림 / 본진 방어 최강

**한 번 고르고 끝나면 실패다.** 전환할 이유가 계속 생겨야 하고, 전환에 몇 초 걸리게 해서 읽고 미리 대비하는 게임으로 만든다.

AI도 써야 한다. `aiEvaluate`가 이미 전술 상태(정면·공성·해상·재편)를 고르므로 거기에 대형 선택을 붙인다. "⚠ 적이 산개 대형으로 전환!" 배너로 가시화한다.

2번과 같은 시스템이므로 함께 설계한다.

### 4. 논리 좌표계 고정 (핵심 수술)
현재 `VW=W*zoom; VH=H*zoom`으로 시뮬레이션 전체가 화면 픽셀에 종속돼 있다. 실측 결과(동일 시드, 화면만 변경):

| 화면 | 결과 | 플레이어 킬 | 열당 인원 |
|---|---|---|---|
| 390×560 | 325s · 3시대 | 36 | 33 |
| 520×950 | 350s · 3시대 | 36 | 42 |
| 1400×800 | 291s · 2시대 | **10** | **113** |

논리 월드 크기를 고정 상수로 분리하고, 화면 픽셀은 카메라 변환에서만 쓴다.

한 번에 셋이 해결된다:
- 2인 대전 desync (기기 크기가 달라도 동일 시뮬)
- 기기별 밸런스 불일치
- PC 와이드 대응의 전제

**밸런스 조정보다 반드시 먼저다.** 좌표계가 밸런스의 기준자다.

### 5. 카메라 — 마우스 휠 줌 + 축소 한계 완화
핀치 줌이 이미 `cam.z`로 구현돼 있다. 휠 이벤트를 같은 경로에 연결한다.

**축소 한계는 유지하되 넉넉하게 푼다.** 현재 `autoCam`에 "최대 축소 = 전장의 62%까지만 / 적 지휘부는 프레임 밖 — 원정을 가야 보인다"는 의도적 제한이 걸려 있다.

**결정: 적 수도는 계속 숨긴다.** "저 너머에 무엇이 있는지 모른다"가 원정의 긴장을 만든다. 무제한 축소는 적진을 처음부터 다 보여주므로 이 설계를 무너뜨린다. 한계치만 완화하고 제한 자체는 남긴다.

**"우주까지 축소"는 채택하지 않는다.** 전장 밖에 아무것도 없어서 끝까지 빼면 검은 여백만 나온다. 그것이 의미를 가지려면 여러 전선·다른 도시·지도가 있어야 하고, 그건 다른 게임이다.

**대신 스케일감은 월드 자동 확장으로 낸다.** 병력이 늘면 월드 크기가 2.6배→5.1배까지 자동으로 자란다(`update()`의 `tws`). PC에서 이 배율 상한을 더 크게 잡으면 "압도적 물량 = 압도적 스케일"이 자연히 나온다.

**성능은 실기 검증 필수.** 병사 하나가 최대 72개 점으로 그려진다. 크게 축소해 한 화면에 수천 개를 그리면 프레임이 무너질 수 있다. 하네스는 렌더를 스텁으로 대체하므로 이 항목은 PC 브라우저에서 직접 확인해야 한다.

### 6. 가로 뷰 (PC 본진)
월드는 세로 그대로 두고 **그리는 방향만 90도 돌린다.** 밸런스가 바뀌지 않는 것이 이 방식의 핵심이다.

진짜 비용은 방향이 있는 그림들이다 — 영웅 3종, 탱크 포신, 지휘관(족장·왕·대통령), 방어탑·성벽·대건축. "위쪽이 앞"이 좌표에 박혀 있어 각각 방향 처리를 다시 해야 한다. 기계적이지만 양이 많다.

### 7. 밸런스 재조정
PC 기준으로. 4번 이후여야 한다.

### 8. 문화 통일 (설계 승인분)
- 대건축 7개 이상 보유 시 "🕊 온 세상의 평화" 해금
- 압도적으로 비쌈 — 누르는 순간 군사적으로 무방비
- 즉시 승리 아님, 건설 시간 필요
- **평화 건설물은 전장에 실재하며 파괴 가능해야 한다.** 이게 없으면 성벽 뒤에서 한 번도 싸우지 않고 이기는 판이 나온다
- AGI/궤도 엘리베이터는 승리 조건이 아니라 **생산성 배수**. 그 돈으로 군대를 뽑을지 평화 비용을 낼지는 플레이어가 정한다
- 대건축은 해당 시대에만 건설 가능하므로 초반부터 문화 노선을 택해야 한다. 이 배타성은 설계 의도다
- 검증 목표: 평화 건설 저지율 3~5할

### 9. 시대 실루엣

---

## 미해결 문제

**미래 시대 콘텐츠가 도달 불가능하다.** 수정된 하네스로 15판(빌드 5 × 난이도 3) 실측:
- 정상 결착 12판: 140~400초(평균 264초), 적 최고 시대 4단계
- 미래 시대(E7) 도달: 3판 — **전부 12분 상한에 걸린 판**
- **핵 기지 0판 / 항공모함 0판 (16판 전부)**

로봇 보병·레일건·스파이더 메카·영웅 타이탄·궤도 엘리베이터·핵·항모가 실제 플레이에서 전혀 나오지 않는다. 단, 하네스 봇이 12판 전패했으므로 판 길이에는 봇 실력 문제가 섞여 있다. **핵·항모 0회는 봇 실력으로 설명되지 않는다 — AI조차 미사일 기지와 항모를 짓지 않는 원인 조사 필요.**

**이전 A/B 데이터 전량 무효.** 함락 연출의 `setTimeout`이 하네스에서 스텁이라 `endGame`이 호출되지 않았고, 지휘부 사망 후에도 시뮬이 12분 상한까지 공회전했다. 그 구간에서 양측이 계속 싸우고 골드·시대가 진행되며 지휘부 체력까지 회복됐다. 교착 관측기도 그 구간을 계속 셌으므로 "교착 128→95초, 26% 감소"라는 결론이 오염된 자로 잰 값이다. 하네스 3종에 "결착 즉시 정지"를 적용해 수정 완료.

**2인 대전이 출시 수준이 아니다.** 공용 MQTT 브로커(broker.emqx.io)를 쓰고 있어 언제 끊길지 모르고, 방 코드가 겹치면 모르는 사람이 들어온다. 1차 출시에서는 "친구 대전"으로 성격을 명확히 하고, 온라인 매칭은 반응을 보고 판단한다.

**CDN 의존.** `mqtt.min.js`를 외부에서 불러온다. 오프라인·래핑 환경에서 로드 실패 가능.

---

## 배포

- 저장소: GitHub (`index.html` + `kit/`)
- 배포: GitHub Pages — `index.html`을 재업로드하면 1~3분 내 자동 반영
- 게임 파일은 **항상 `index.html`이라는 이름으로** 산출한다. 버전은 파일명이 아니라 커밋 이력이 관리한다
- 업로드는 확인된 버전만. 저장소에는 작동하는 버전만 남긴다
