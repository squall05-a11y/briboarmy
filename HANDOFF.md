# 인수인계 — ARMY EVOLUTION

이 문서는 설계·검수 세션의 전체 맥락을 넘기는 문서다.
`CLAUDE.md`(협업 규칙)와 `ROADMAP.md`(작업 순서)를 함께 읽어라.

---

## 1. 프로젝트

세로 화면 1v1 문명 전쟁 게임. 단일 HTML 파일, 약 197KB. Age of War 계열에 4X를 상당량 이식했다.

**8시대**(석기→미래), 병종 40여 종, 건물 11종, 해군 레이어, 영웅 4종, 대건축 8종, 핵 개발, 개국 이념 3택, 트로피 메타, 2인 대전(MQTT 락스텝).

**설계상 가장 좋은 부분:** 시대 진급이 추상 게이지가 아니라 전장에 실재하는 부술 수 있는 건물(기념비)이다. "적의 발전을 막으려면 때리러 가야 한다"가 테크와 전투를 하나의 판 위에 올린다. 승급 시스템(5기→분대→소대→중대)은 오브젝트 수를 130으로 묶으면서 화면 밀도는 수백 명처럼 보이게 하는 모바일 성능 해법이다.

---

## 2. 협업 구조

사용자는 **한국 시나리오 작가이고 코드를 읽지 못한다.** 이것이 모든 규칙의 전제다.

| 역할 | 담당 |
|---|---|
| 설계 판단·검수·지시서 작성 | 채팅 세션 |
| 코드 수정·하네스 실행 | Claude Code |
| 심층 분석·원인 추적 | Cowork |
| 파일 보관·이력·배포 | GitHub |

**상시 규칙 5**(CLAUDE.md에 명문화, 위반 = 사고):
1. 변경 전량 보고 — 사소한 계측 필드 추가까지
2. 요청 밖 변경은 먼저 문의
3. 밸런스 수치 무단 변경 금지
4. 스코프 축소·연기는 명시
5. 사용자는 코드를 읽지 못한다 — 보고되지 않은 변경은 영원히 발견되지 않는다

**이 규칙은 실제 사고에서 나왔다.** 이전 세션에서 보고 누락이 4회 발생했다(뗏목 수치 변경, 연안 교전 규칙 추가, 하네스 출력 필드 추가 2회). 사용자가 코드를 못 읽으므로 채팅 세션이 매번 코드를 대조해 잡아냈다. 지속 불가능한 방식이라 규칙으로 전환했다.

---

## 3. 환경 현황

**저장소:** GitHub `squall05-a11y/briboarmy`
**배포:** GitHub Pages — `index.html` 재업로드 시 1~3분 내 자동 반영
**게임 파일명은 항상 `index.html`.** 버전은 파일명이 아니라 커밋 이력이 관리한다.

**⚠ Claude Code에 저장소 쓰기 권한이 없다.** GitHub 앱이 계정에 설치되지 않아 OAuth 읽기만 되고 push가 403으로 막힌다. 해결을 시도했으나 실패했고, **우회로로 확정했다.**

> Claude Code가 완성된 파일을 첨부로 내놓는다 → 사용자가 GitHub 웹에서 업로드 → Pages 자동 반영

푸시 시도는 시간 낭비다. 요구하지 마라. 이력·diff는 GitHub이 담당하므로 감사 안전성은 동일하다.

**검증 도구:** 하네스 4종이 저장소 루트에 평면 배치. 실행법은 `HARNESS.md`. node 환경에서 게임의 `<script>`를 추출해 DOM·오디오·네트워크를 스텁으로 씌우고 실제 시뮬레이션 코드를 구동한다.

**하네스 한계 3 (항상 명시할 것):**
1. `setTimeout`·렌더가 스텁 — 연출 의존 판정은 보정 판독
2. 입력이 봇 — 사람 플레이 패턴과 다름
3. **UI 계층 사각지대** — 터치·버튼 표시·iOS 오디오는 실기로만 검증 가능

---

## 4. 확정된 설계 결정

**주 플랫폼은 PC.** 넓은 전장, 휠 줌, 압도적 물량, 긴 판, 미래 시대까지 가는 문명 레이스 — 전부 PC 감각이다. 폰은 같은 코드의 세로 모드로 남는다.

**코드는 한 벌.** 프로젝트를 둘로 나누지 않는다. 시뮬레이션은 세로축을 유지하고 PC에서는 화면만 눕혀 그린다. 월드가 고정되므로 화면이 달라도 밸런스가 동일하다.

**RTS식 개별 조작은 넣지 않는다.** 병력이 1000을 넘고 승급 시스템상 개별 병사가 조작 단위로 존재하지 않는다. 대신 **클릭 하나로 전군 대형을 바꾼다**(밀집 방진 / 산개 횡대 / 쐐기 돌격 / 종심 방어, 가위바위보 성립).

**적 수도는 화면 밖에 둔다.** 카메라 축소 한계를 완화하되 제거하지 않는다. 원정을 가야 적진을 본다.

**시간 강제 종료 장치는 부활시키지 않는다.** 짧게 끝나도 좋고 40분이 되어도 좋다. 교착은 시계가 아니라 실제 정체 상태로 감지한다.

**"나중에 폰으로 간추린다"에 의존하지 않는다.** 기능 추가 시점에 PC 전용인지 공용인지 그 자리에서 정한다.

---

## 5. 이미 완료된 수술

**시간 강제 종료 장치 제거.** 사용자가 요청한 적 없는 4개의 시계(300s 건물 자괴·수리 차단, 420s 수입 감쇄, 540s 매 프레임 강제 돌격, 300s 총력전 배너)가 누적돼 있었다. 540초 장치는 플레이어의 대기·후퇴 조작을 매 프레임 덮어써 조작권을 박탈했다. 전부 `stallStage` 변수로 이관했고 현재 항상 0이다.

**AI 자기 평가 루프 추가.** `aiEvaluate`가 20초(헬 15초)마다 교환비·전선 전진·적 기념비 진행을 재고, 실패 2연속이면 전술을 바꾼다(front → siege / regroup → navy).

**AI 해군 제외 해제.** `aiThink`의 고용 목록에서 `!u.naval`로 함선이 통째로 배제돼 있었다.

**연안 교전 재구현.** 지상 원거리가 함선을 표적으로 삼되, 이동 판단에서는 제외해 전열이 바다로 끌려가지 않게 했다.

**하네스 좀비 구간 수정.** 아래 참조.

---

## 6. 검증된 문제 (전부 실측 확인)

### 6-1. 함선 유입 — 필터 누락 3곳

조선소 없이 함선이 새어 들어온다.

- `triadCards` 인구 풀 — `!u.naval` 없음. **시대 6에서 인구 풀의 33%가 함선**이고 그중 💰3,500 항공모함 포함
- `applyItem` 보급 상자 🎁 풀 — 없음
- `onEraUp` **AI 시대 보상 풀** — 없음. 같은 함수 안에서 플레이어 풀은 함선을 제외한다. **AI만 공짜 함선을 받는 비대칭**
- `slotEval` — 같은 누락이 있으나 `openSlot` 호출부가 없는 **죽은 코드 확정**

### 6-2. AI 해군은 산 것이 아니라 새어 들어온 것 ← 가장 중요

9판 실측(빌드 3 × 난이도 3), 조선소 수를 직접 계측:

```
E조선소 0 | E해군 9 | E항모 1   ← 조선소 없이 항공모함
E조선소 0 | E해군 7
E조선소 0 | E해군 3
... 9판 전부 조선소 0채
AI 전술 도달: regroup까지만. navy 도달 0판
```

**조선소는 `ev.tac==="navy"`일 때만 짓는데 그 상태에 도달하지 못한다.** 초기값 `front`에서 navy까지 가려면 전환 2단계, 각 단계가 "평가 2회 연속 실패"를 요구해 60~80초의 연속 열세가 필요하다.

**따라서 6-1을 고치면 AI 해군은 0이 된다.** 바다·제해권·해안포·항모 소티가 전부 플레이어 전용 콘텐츠가 된다. 지금은 버그로나마 돌아가던 것이 아예 죽는다.

### 6-3. 전술 시스템 전면 단절

`TACTICS`에 시대별 5종(일제사격·화살 폭풍·전군 돌격·폭격 요청·탄막 집중)이 완성돼 있으나 **`useTactic()`을 호출하는 곳이 코드 어디에도 없다.** 실측 호출 0회.

부수 문제:
- **쿨다운이 두 값.** `useTactic`은 16~26초, 버튼은 하드코딩 40초. 연결하는 순간 필살기 회전율이 1.5~2.5배 빨라진다
- **거짓 정보 메커니즘.** 버튼 라벨은 `doctrine`(항목 1개 → 항상 "일제사격"), 진화 토스트는 `tacticSel`. 서로 다른 변수를 읽는다. 시대가 올라도 실제로는 안 바뀌면서 "진화했다"고 표시한다
- **`DOCTRINES` 확장이 지뢰.** `startGame`에서 `DOCTRINES[1]`, `[2]`를 참조하는데 배열엔 0번뿐이라 `undefined`가 폴백에 가려져 있다. 항목을 3개로 늘리는 순간 AI 독트린이 예고 없이 살아난다

### 6-4. 미래 시대·핵·항모 도달 불가

수정된 하네스로 15판 실측(빌드 5 × 난이도 3):

| | 결과 |
|---|---|
| 정상 결착 | 12판, 140~400초 (평균 264초) |
| 12분 상한 도달 | 3판 |
| 미래 시대(E7) 도달 | 3판 — **전부 상한에 걸린 판** |
| 핵 기지 / 항공모함 | **0판 / 0판 (16판 전부)** |

정상 결착 판의 적 최고 시대는 4단계였다.

**원인 특정:** `aiUtilityAct`의 후보는 정확히 셋 — 영웅 고용, 영웅 강화, 대건축. **미사일 기지·아이언돔·대공포·핵 요격에 대한 후보가 아예 없다.** 확률이 낮은 게 아니라 0이다.

로봇 보병·레일건·스파이더 메카·영웅 타이탄·궤도 엘리베이터가 실제 플레이에서 전혀 나오지 않는다.

**교란 요인:** 하네스 봇이 12판 전패했으므로 판 길이에는 봇 실력 문제가 섞여 있다. 다만 **핵·항모 0회는 봇 실력으로 설명되지 않는다** — 코드 경로가 없다.

### 6-5. 시뮬레이션이 화면 크기에 종속

`VW=W*zoom; VH=H*zoom`. 동일 시드, 화면만 변경한 실측:

| 화면 | 결과 | 플레이어 킬 | 열당 인원 |
|---|---|---|---|
| 390×560 | 325s · 3시대 | 36 | 33 |
| 520×950 | 350s · 3시대 | 36 | 42 |
| 1400×800 | 291s · 2시대 | **10** | **113** |

와이드에서 병사가 한 줄에 113명씩 퍼져 근접 전투가 성립하지 않는다. **PC 대응·2인 대전 desync·기기별 밸런스 불일치가 전부 이 하나의 원인에서 나온다.**

### 6-6. 기타

- **2인 대전이 출시 수준 아님.** 공용 MQTT 브로커(broker.emqx.io) 사용. 방 코드가 겹치면 모르는 사람이 들어온다
- **CDN 의존.** `mqtt.min.js`가 외부 로드. 차단 상태에서 1인전은 정상, **2인전만 영향**
- **죽은 코드 잔재.** `castUlt`의 `return` 뒤 도달 불가 블록, `advanceCiv`의 `if(false)` 블록, CSS 중복 정의 — 채팅으로 5천 줄을 반복 재작성한 흔적

---

## 7. 무효화된 데이터 ⚠

**이전 A/B 실측은 전량 무효다.**

함락 연출의 `setTimeout`이 하네스에서 스텁이라 `endGame`이 호출되지 않았고, 지휘부 사망 후에도 시뮬이 12분 상한(720초)까지 공회전했다. 그 구간에서 양측 군대가 계속 싸우고 골드·시대가 진행되며 죽은 쪽 지휘부 체력이 초당 5씩 회복됐다. 교착 관측기도 그 구간을 계속 셌다.

**무효가 된 결론:**
- "평균 판 길이 696초" — 게임 길이가 아니라 상한의 그림자
- "교착 128→95초, 26% 감소" — 오염된 자로 잰 값
- "11~12분에 미래 시대 도달" — 좀비 구간의 산물
- "B군 해군 사용률 93% — 해상 우회가 실전 레퍼토리로 정착" — **실제로는 누출된 함선이었다**

하네스 3종에 "결착 즉시 정지"를 적용해 수정 완료. **이후 모든 측정은 이 자로 잰다.**

---

## 8. 지금 대기 중인 결정 ← 여기서 이어받아라

**작업 1(함선 유입 필터)의 범위를 정해야 한다.**

필터만 막으면 6-2에 따라 **AI 해군이 확실히 0이 된다.** 버그를 고쳤는데 콘텐츠 한 층이 죽는 상황이다.

선택지:

**A. 필터만 막는다 (원래 범위)**
- 장점: 원인과 결과가 분리돼 A/B가 깨끗하다. 버그 수정과 밸런스 변경이 섞이지 않는다
- 단점: 다음 작업 전까지 AI 해군이 없다

**B. AI 건설 후보 추가를 같이 한다**
- `aiUtilityAct`에 조선소를 추가해 `navy` 전술을 거치지 않아도 짓게 한다
- 미사일 기지·아이언돔·대공포·요격시설도 후보에 추가하면 6-4(핵·항모 0회)가 같이 풀린다
- 단점: **밸런스 변경이므로 사용자 승인 필요.** 그리고 A/B 해석이 복잡해진다

**채팅 세션의 권고는 A → B 분리다.** 필터를 먼저 막고 기준선을 뽑은 뒤, AI 건설 경로를 작업 1.5로 별도 측정한다. 원인과 결과를 섞지 않는 것이 지금까지 이 프로젝트가 겪은 사고의 반대편이다.

다만 사용자가 "해군이 잠시 죽는 것"을 받아들일지는 확인이 필요하다.

---

## 9. 작업 순서 (ROADMAP.md 참조)

1. **인구 카드 함선 유입 필터** ← 지금 여기
2. 전술 시스템 재연결 (값싸고 효과 즉시)
3. 작전·대형 시스템 (2와 같은 시스템, 함께 설계)
4. **논리 좌표계 고정** — PC 대응·desync·기기별 밸런스가 한 번에 해결. **밸런스 조정보다 반드시 먼저**
5. 마우스 휠 줌 + 축소 한계 완화
6. 가로 뷰 (PC 본진)
7. 밸런스 재조정
8. 문화 통일
9. 시대 실루엣

---

## 10. Claude Code로 넘기는 법

Cowork에서 분석·설계가 끝나면 **작업 지시서를 마크다운 파일로 산출**해라. 사용자가 GitHub에 올리고, Claude Code에 한 줄만 보낸다:

> TASK-XX.md 를 읽고 지시대로 진행해라.

**지시서에 반드시 포함할 것:**
- 시작 전 읽을 문서 (`CLAUDE.md`, `ROADMAP.md`, `HARNESS.md`) + **읽은 내용 요약 보고 후 승인 대기**
- 문제 기술 — 위치와 원인을 자연어로
- 작업 범위 — **"이것 외에는 아무것도 수정하지 마라. 눈에 띄는 문제는 고치지 말고 보고만 해라"**
- 제약 — 밸런스 수치·시대 진급 시간·`draw()` 렌더링 금지
- 검증 — `node --check` → `harness23` → `harness27_2p` → `harness30` A/B. **표본 수와 완주 여부 명시**
- 보고 — 한국어, 변경 목록 전량
- 산출 — **완성된 파일을 첨부로.** 푸시 시도 금지

**작업 1 지시서(`TASK-01.md`)는 이미 작성돼 저장소에 있다.** 다만 8절의 범위 결정에 따라 수정이 필요할 수 있다.

---

## 11. 채팅 세션이 이 인수인계에서 인정한 것

Cowork의 직전 분석이 채팅 세션보다 정확했다. 채팅 세션은 누출 지점 3곳과 전술 단절을 먼저 찾았으나, **자기 하네스 출력에 "E해군 22"가 찍히는 것을 보고도 그 배들이 어디서 왔는지 묻지 않았다.** 해상 우회 전술이 작동하는 것으로 넘겨짚었다.

Cowork는 증상이 아니라 원인을 추적했고, 핵·항모 0회의 기계적 이유(`aiUtilityAct` 후보 부재)까지 특정했다. 채팅 세션은 "조사하라"로 넘긴 지점이다.

**이 프로젝트에서 반복되는 실패 패턴은 "그럴듯한 설명에서 멈추는 것"이다.** 숫자가 나왔을 때 그 숫자가 어디서 왔는지 한 단계 더 묻는 것 — 그것이 지금까지 발견된 모든 문제를 찾아낸 방법이다.
