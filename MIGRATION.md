# 이사 절차 — 폰 전용 (컴퓨터·터미널 불필요)

## 1. 저장소 만들기 (약 5분, 1회)
1. 폰 브라우저에서 github.com 접속 → 계정 생성(무료).
2. 우상단 + → **New repository** → 이름 `army-evolution` → Public → Create.
3. **Add file → Upload files** → 이 키트의 파일 전부 + 게임 `index.html` 업로드 → Commit.
   - 이후 모든 수정 = 같은 화면에서 같은 이름으로 재업로드(= 자동으로 새 커밋, 이력·diff가 영구 기록).

## 2. 자동 배포 — GitHub Pages (무료·결제 불요, 약 3분, 1회)
1. 저장소 화면 → **Settings** → 왼쪽 메뉴 **Pages**.
2. "Build and deployment" → Source: **Deploy from a branch** → Branch: **main / (root)** → Save.
3. 1~3분 뒤 `https://<계정명>.github.io/army-evolution/` 접속 = 게임 실행.
   - 파일명이 **index.html**이어야 루트에서 열림(이 키트의 게임 파일이 그 이름).
   - 이후 index.html을 재업로드(커밋)할 때마다 자동 재배포(1~3분). 캐시 미스터리 구조적 해소.

## 3. Claude 연결
- 경로 A(권장): Claude 앱 설정 → 연결(Connectors)에서 GitHub 연동이 보이면 연결 → 저장소를 대화에서 직접 열람·작업.
- 경로 B(연동이 없거나 제한 시): 지금과 동일하게 대화에 파일 첨부 → Claude가 수정본 산출 → 사용자가 GitHub에 재업로드. **감사(diff·이력)는 GitHub이 자동 담당**하므로 협업 안전성은 동일하게 확보됨.
- 정직 고지: 경로 A의 가용 여부·세부는 앱 버전에 따라 다를 수 있음. 막히면 그 자리에서 최신 절차를 검색해 안내한다.

## 4. 테스트 루프 (수정 → 확인)
수정 요청 → Claude가 파일 산출(1턴) → GitHub 업로드(탭 2회) → GitHub Pages 자동 배포(1~3분) → 배포 URL 새로고침.
**총 4단계, 체감 2~3분.** 기존 아티팩트 즉시 플레이보다 한 단계 느리지만, 버전 스탬프 없이도 항상 최신본이 보장됨.

## 5. 첫 작업 (승인분)
인구 카드 함선 필터 1줄 수정 → 하네스 A/B 재실측 → 커밋. 이 한 바퀴로 워크플로 감을 잡은 뒤 대공사(시대 실루엣·문화 통일) 진행.
