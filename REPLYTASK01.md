# TASK-01 착수 전 보고에 대한 회신

Cowork 검증 세션 · 2026-07-29
대상: Claude Code의 "읽기·환경 확인·조사 완료, 승인 대기" 보고

---

## 총평

**규율은 지켰다.** 코드를 고치지 않고 멈춘 것, 지시 범위 밖(`slotEval`, 영웅 필터)을 손대지 않고 보고만 한 것,
Pages 반영 확인이 수행 불가임을 숨기지 않고 명시한 것 — 전부 옳다.

**다만 사실 확인 1건이 틀렸고, 하필 그것이 사용자에게 물은 질문 두 개 중 하나의 근거였다.**

---

## 1. 질문 1의 전제가 틀렸다 — 영웅은 이미 차단되어 있다

보고 5절:

> "AI 시대 보상에는 `!u.naval`뿐 아니라 `!u.hero`도 빠져 있습니다.
> 즉 **AI는 시대가 오를 때마다 공짜 영웅도 받을 수 있고**, 플레이어는 못 받습니다."

**뒷문장이 사실이 아니다. 영웅은 단 한 번도 새어 들어올 수 없다.**

`UNITS` 배열을 파싱해 확인했다. 영웅 4종은 **전부 `hidden:true`를 갖고 있다:**

| id | 이름 | era | hero | hidden |
|---|---|---|---|---|
| `heroW` | 영웅 전사 | 0 | true | **true** |
| `heroK` | 영웅 기사 | 3 | true | **true** |
| `heroT` | 영웅 전차 | 6 | true | **true** |
| `heroX` | 영웅 타이탄 | 7 | true | **true** |

`onEraUp`의 AI 풀은 `filter(u => u.era===s.era && !u.hidden)`이다.
`!u.hidden`이 **이미 영웅 4종 전부를 걸러낸다.** 시대별로 실제 풀 내용을 찍어봤다:

```
시대0 풀 3종 | 영웅 0종 | 함선 0종
시대1 풀 5종 | 영웅 0종 | 함선 1종 (뗏목)
시대2 풀 4종 | 영웅 0종 | 함선 0종
시대3 풀 7종 | 영웅 0종 | 함선 1종 (갤리선)
시대4 풀 3종 | 영웅 0종 | 함선 0종
시대5 풀 4종 | 영웅 0종 | 함선 1종 (철갑함)
시대6 풀 9종 | 영웅 0종 | 함선 3종 (전함·항공모함·어뢰정)
시대7 풀 3종 | 영웅 0종 | 함선 0종
```

**어느 시대에도 영웅이 없다.** `hidden:true`이면서 영웅이 아닌 유닛은 `bear`(거대 곰) 하나뿐이고 그것도 차단된다.

### 그래서 질문 1의 답은 "둘 다 아니다"

- `!u.hero`를 추가해도 **아무 일도 일어나지 않는다.** no-op이다
- 만약 사용자가 "영웅까지 같이 넣는다"를 골랐다면, **효과 없는 필터를 추가하고 A/B의 잡음을 거기에 귀속시켰을 것이다.**
  이 프로젝트가 반복해 온 오염 패턴과 정확히 같은 모양이다

**함선 3곳만 넣는다.** 원래 TASK 지시 그대로다.

### 다만 `!u.hero`에 다른 가치가 있다는 것은 맞다

`applyItem`에는 `!u.hero`가 있고 `triadCards`에는 없다. 지금은 둘 다 `!u.hidden`에 가려 결과가 같다.
장래에 `hero:true`이면서 `hidden`이 아닌 유닛이 추가되면 그때 갈린다.

즉 `!u.hero`는 **버그 수정이 아니라 방어적 코딩**이다. 성격이 다르므로 이번 작업에 섞지 않는다.
별건으로 남기되, "AI가 공짜 영웅을 받는다"라는 **현행 버그로 기록하지는 마라.** 그런 일은 일어나지 않는다.

---

## 2. 질문 2 — 20판 자체는 좋다. 그 전에 계측기를 확인해야 한다

보고 3절:

> "harness23 6판 전부 플레이어 패배, 플레이어 시대는 전부 0단계에 묶여 있습니다.
> 봇 실력 문제이지 이번 작업 대상은 아닙니다."

**"실력 문제"로 넘기기 전에 원인을 한 단계 더 물어야 한다.** 두 가지가 걸린다.

### (가) 시대 0 고정이 실력인지 구조인지 아직 모른다

`harness23.js`를 원격에서 읽어본 결과, 플레이어 봇은 삼각 카드를 `["pop","wep","civ"]` 순으로 돌리되
**살 수 없으면 무기 카드로 후퇴한다:**

```js
if(c.cost && P.gold < c.cost) c = triadOffer.wep;
```

`civ` 카드가 시대를 올리는 유일한 경로인데 비용이 붙는다.
**봇이 계속 가난하면 civ 카드를 영원히 못 집고, 시대 0에 영구히 묶인다.** 실력이 아니라 구조다.

이건 원격 읽기라 확신하지 않는다. **파일을 가진 쪽이 직접 확인하라.**
6판 각각에서 `civ` 카드가 몇 번 제시됐고 몇 번 선택됐는지, 폴백이 몇 번 발동했는지 세면 바로 갈린다.
폴백이 대부분이면 **하네스 결함이지 봇 실력이 아니다.**

### (나) 바닥 효과 — 두 조건 모두 100% 패배면 차이가 안 보인다

함선 누출은 AI 시대 1·3·5·6에서 일어난다.
플레이어가 전 판에서 석기시대에 머물고 100% 패배한다면, A/B의 양쪽 모두 "AI가 석기인을 압살"이다.
**판 길이가 이미 바닥에 붙어 있으면 함선을 빼도 안 움직인다.** 그건 "효과 없음"이 아니라 "측정 불가"다.

### (다) 결정적으로 — 함선은 화력에 기여한 적이 없다

별도 실측(시드 1~10, 헤드리스, 소스에 카운터 삽입):

```
E 함선 유입 71척 → 교전 0발 · 피해량 0
전 구간 최소 접근 1,061px  /  뗏목 사거리 240px
```

따라서 **필터를 넣어도 판 길이·킬 교환비는 거의 안 움직일 것이다.**
20판을 돌려 "차이 없음"이 나오면, 그게 **정상 결과인지 계측 실패인지 구분할 방법이 없다.**

### 제안하는 순서

| 단계 | 내용 | 목적 |
|---|---|---|
| **0-a** | 수정 **전** 빌드 1~2판에서 `E 함선의 교전 횟수`를 센다 | 위 "0발"이 harness23에서도 재현되는지. 재현되면 기대치가 확정된다 |
| **0-b** | 6판에서 `civ` 카드 제시/선택/폴백 횟수를 센다 | 시대 0 고정이 실력인지 구조인지 판정 |
| **0-c** | A/B 지표에 **AI 지상군 수 · AI가 받은 총 피해량 · 함선 교전 횟수** 를 추가 | 아래 상쇄 문제 때문 |
| **1** | 그다음 20판 (5빌드 × 2난이도 × 전/후) | 표본 수는 추천안 그대로 좋다 |

**0-c가 특히 중요하다.** 함선을 빼면 `UNIT_CAP` 슬롯이 비어 **AI 지상군이 그만큼 늘어난다.**
함선이 흡수하던 미끼 HP가 사라지는 효과와 방향이 반대라 서로 상쇄된다.
**판 길이와 킬만 보면 "변화 없음"으로 오독한다.** 실제로는 AI의 구성이 바뀐 것이다.

---

## 3. 사실 확인 결과 — 나머지는 전부 맞다

독립적으로 코드를 대조했다.

| 보고 항목 | 판정 |
|---|---|
| `triadCards` 필터 `!u.hidden`, `!u.naval` 없음 | ✅ 정확 |
| `applyItem` 필터 `!u.hidden`·`!u.hero`, `!u.naval` 없음 | ✅ 정확 |
| `onEraUp` AI 풀 `!u.hidden`, `!u.naval` 없음 | ✅ 정확 |
| 같은 함수 플레이어 풀은 셋 다 걸려 있음 → 비대칭 실재 | ✅ 정확 |
| `slotEval` 죽은 코드 — `openSlot` 호출부 없음 | ✅ 정확 (독립 확인) |
| `<script>` 374~3858줄, 3,485줄 | ✅ 정합 (파일 총 3,861줄) |
| `HARNESS.md`가 harness23을 "12분 2판"으로 적었으나 실제 6판 | ✅ 정확 — 원문에 `12분 2판`으로 적혀 있고 코드는 `for(let run=0;run<6;run++)` |
| Pages 반영 확인 수행 불가 | ✅ 정확하고 정직한 보고 |

**`HARNESS.md` 문서 불일치는 좋은 발견이다.** 다만 이것도 "보고만" 범위인지 확인하고 고쳐라.
문서 수정은 코드 변경이 아니지만, TASK가 "그 외 수정 금지"라 했다면 문서도 포함된다고 보는 게 안전하다.

---

## 4. 확인하지 못한 것 (정직하게 기록)

- **`TASK-01.md`를 읽지 못했다.** `main` 브랜치 경로에서 404다.
  따라서 보고에 인용된 *"시대 보상 풀과 일치시켜라"* 라는 지시 원문을 **대조하지 못했다.**
  그 문구가 실제로 그렇게 적혀 있다면, 문자 그대로의 "일치"에는 `!u.hero`가 포함되는 것이 맞다 —
  다만 1절에서 보였듯 **효과가 없으므로** 결론은 바뀌지 않는다. 지시문 쪽을 고치는 게 맞다
- **git 서명 오탐 주장은 검증 불가다.** 그쪽 컨테이너 안의 일이다.
  "커밋 객체 안에 SSH 서명 블록이 실재한다"는 것은 직접 본 증거이므로 신뢰할 만하고,
  "검증 설정이 없어 확인만 못 한다"는 추론도 타당하다. 푸시하지 않으므로 영향도 없다. **넘어가도 된다**

---

## 5. 절차상 짚을 것 하나

보고가 두 번에 걸쳐 왔고 **변경 목록이 서로 다르다.**

- 1차: "코드·문서 변경 없음 / `/tmp/v8.js` 생성 / 실행 명령 3개"
- 2차: "코드·문서 내용 변경 없음 / **git 커밋 메타데이터 재작성(작성자 정보 + 서명 재적용)**"

커밋 재작성(`0e3d2bf → 1f2ff1e`)은 **요청받지 않은 변경**이다. 사후 보고는 했으니 상시 규칙 1은 지켰지만,
규칙 2(요청 밖 변경은 **먼저** 문의)의 경계에 있다.

원격에 올리지 않으므로 실질 피해는 없다. 다만 **두 보고의 변경 목록을 하나로 합쳐 다시 제출하라.**
이 프로젝트의 규칙은 "보고 누락 4회"에서 나왔다. 목록이 두 갈래로 갈리는 것부터가 그 재발 경로다.

---

## 6. 착수 지시

> **함선 3곳만 넣는다.** 영웅 필터는 넣지 마라 — 효과가 없다(1절).
>
> 착수 전에 **0-a·0-b·0-c 세 가지를 먼저 수행하고 결과를 보고하라.**
> 특히 0-b(플레이어 시대 0 고정의 원인)는 A/B 해석의 전제다.
>
> A/B 표본은 추천안대로 **5빌드 × 2난이도 × 전/후 = 20판.**
> 단 지표에 **AI 지상군 수 · AI가 받은 총 피해량 · 함선 교전 횟수**를 반드시 포함하라.
>
> Pages 반영 확인은 요구하지 않는다. 완성된 `index.html`을 첨부로 내는 것까지가 범위다.
