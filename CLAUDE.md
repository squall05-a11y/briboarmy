# ARMY EVOLUTION — 협업 헌법 (CLAUDE.md)

이 저장소의 모든 세션은 이 문서를 먼저 읽고 따른다.

## 상시 규칙 5 (위반 = 사고)
1. **변경 전량 보고**: 매 턴 말미에 "변경 목록"으로 파일별 수정 사항 전부를 나열한다. 보고 없는 변경은 존재하지 않는 것으로 간주된다.
2. **요청 밖 변경은 먼저 문의**: 요청되지 않은 기능·룰·수치는 구현 전에 제안하고 승인을 받는다.
3. **밸런스 수치 무단 변경 금지**: 유닛/건물의 체력·공격·비용·시대 진급 시간(ERA_BASE, MONU_NEED)은 명시 승인 없이 건드리지 않는다.
4. **스코프 축소·연기는 명시 보고**: "일단 뺐다"는 반드시 그 턴에 문장으로 알린다. 사용자는 코드를 읽지 못하므로 보고되지 않은 누락은 영원히 발견되지 않는다.
5. **사용자는 코드를 읽지 못한다**: 모든 판단 근거를 자연어로 설명한다. 코드 인용으로 설명을 대체하지 않는다.

## 검증 의무
- 모든 패치는 배포 전: `node --check` → `harness23`(스모크) → `harness27_2p`(2인 동기) 통과 필수.
- 밸런스·AI 변경은 `harness30`(봇 매트릭스, BUILD/D 환경변수) 실측 동반.
- **하네스의 한계 3 (항상 명시)**: ① setTimeout·렌더 스텁 — 연출 의존 판정(함락 시퀀스)은 지휘부 체력 기준으로 보정 판독 ② 입력이 봇 — 사람 플레이 패턴 아님 ③ **UI 계층 사각지대** — 터치·버튼 표시·iOS 오디오는 실기 스샷으로만 검증 가능. 하네스 통과 ≠ 실기 무결.

## 보고 형식
- **한국어**, 코드 용어 최소화. 짧고 직설적으로.
- 큰 변경 전 "회의": 질문 + 추천안 병기 → "추천대로" 승인 후 착수.
- **승인 전 코드 수정 금지**. 조사·설계 보고가 먼저다.

## 파일 구조
- `index.html` — 게임 본체 (단일 파일)
- 하네스·문서는 저장소 루트에 평면 배치 — `harness23.js` / `harness27_2p.js` / `harness30.js`, `diag.js`, `runAB.sh`, `HARNESS.md`, `MIGRATION.md`
- 하네스 실행: 게임에서 `<script>` 추출 → `/tmp/v8.js` → `node harness23.js`

## 현재 상태 요약 (이사 시점)
8시대 엔진 / 바다·해군·제해권·항모 소티 / 미사일 기지·아이언돔·대공포 / 함락전 30초 / 개국 이념 3택 / AI 자기평가 루프(전술 전환·해상 우회) / 시간 강제 종료 장치 제거(stallStage 관 유지, 상시 0) / 교착 관측기(계측 전용).
미해결·대기: 인구 카드 함선 유입 필터(첫 작업 예정), 문화 통일 시공(설계 승인분), 시대 실루엣, 안전망 축소판 여부(데이터 후 결정).
