# 코워커 하네스 결정성 검사 — 실패 보고

2026-07-29 · 오퍼스의 비결정성 지적에 대한 자기 점검

---

## 결론: 내 하네스도 비결정적이었다. 지적이 맞다.

동일 시드(4242), 동일 화면(520×950), **실행 리듬만 바꿔서** 3회:

| 회차 | Math.random 호출 | AI 시대 | AI 골드 | AI 지휘부 HP | 병력 |
|---|---|---|---|---|---|
| 1 | 215,053 | 3 | 847 | 12,583 | 50 |
| 2 | 213,564 | 2 | 304 | 9,830 | 42 |
| 3 | 224,141 | 2 | 194 | 9,830 | 49 |

**같은 시드에서 결과가 다르다.** 이 세션에서 내가 낸 모든 실측값은 이 자로 잰 것이다.

---

## 원인 셋 — 크기 순

### 1. 사운드가 난수를 훔친다 (오퍼스가 지목한 것) — 전체의 98%

```js
function noiseS(dur,vol,delay){
  if(muted||!AC)return;
  ...
  for(let i=0;i<len;i++) d[i]=(Math.random()*2-1)*(1-i/len);   // ← 버퍼 길이만큼 소비
}
```

**헤드리스 크로미움에도 `AudioContext`가 존재한다.** 검사에서 `[object AudioContext]`로 확인했다.
node 스텁 하네스라면 `!AC`로 걸러지겠지만, 실제 브라우저를 쓰는 내 하네스는 그대로 통과했다.

호출량이 `rl(k,ms)`의 `performance.now()` 게이팅을 받으므로 **실행 속도에 따라 훔치는 양이 달라진다.**

**대책:** `muted = true` 한 줄. `noiseS`·`tone` 모두 첫 줄에서 반환한다.
→ Math.random 호출 **215,000 → 4,500 (98% 감소)**

### 2. 게임의 rAF 루프가 수동 스텝과 동시에 돌고 있었다 — 내가 새로 찾은 것

`loop()`이 `requestAnimationFrame`으로 계속 돌면서 `running`인 동안 `update(dt)`를 호출한다.
**내 수동 `update(1/30)` 루프와 나란히 시뮬을 진행시키고 있었다.** 벽시계 속도로.

즉 내가 "300초 돌렸다"고 기록한 판은 실제로는 300초 + rAF가 밀어넣은 미지의 양만큼 진행돼 있었다.

**대책:** 페이지 로드 전에 `window.requestAnimationFrame = ()=>0;`
→ 결과 분산 **12% → 1%**

### 3. `setTimeout` 콜백이 다른 시뮬 시점에 끼어든다 — 잔여, 미해결

위 둘을 다 막고도 여전히 어긋난다 (4,496 / 4,515 / 4,541).

하네스가 타이머 숨통을 틔우려고 `await`로 양보하는데, **양보 리듬이 달라지면 게임의 대기 중인 `setTimeout` 콜백이 서로 다른 시뮬 시각에 실행된다.**
분산은 작지만(±0.5%) 시뮬레이션이 혼돈계라 결과가 갈린다.

**대책 (미검증):** 양보 리듬을 완전 고정하거나, `setTimeout`을 결정론적 큐로 스텁한다.
오퍼스의 `performance.now` 고정 증분 스텁은 원인 1의 게이팅에는 듣지만 **이 세 번째는 못 잡는다.**

---

## 그래서 무엇이 살아남는가

### ✅ 유효 — 난수와 무관한 정적 분석

| 내용 | 근거 |
|---|---|
| 함선 유입 필터 누락 3곳 + `slotEval` 죽은 코드 | 코드 정독 |
| `useTactic()` 호출부 0곳 | 전체 검색 |
| 영웅 4종 전부 `hidden:true` → `!u.hero`는 no-op | `UNITS` 배열 파싱 |
| 시대별 함선 유출 비중 (20 / 14 / 25 / **33%**) | `UNITS`에서 직접 계산 |
| `aiUtilityAct` 후보가 영웅·영웅강화·대건축 셋뿐 | 코드 정독 |
| 대건축 `fy` 0.16→0.07 vs 지휘부 `VH*0.06` | 코드 정독 |
| **`#app { max-width:520px }`** — PC 렌더 폭 상한 | CSS 정독 |
| `resize()` `VW=W/zoom` vs `update()` `VW=W*zoom` | 코드 정독 |
| 함선이 `fireShot`이 아니라 `dealTo`를 쓴다 | 코드 정독 |

### ❌ 폐기 — 전부 오염된 자로 잰 값

- "AI 함선 71척 / 교전 0발"
- "전 구간 최소 접근 1,061px" · "플레이어 함대 441px"
- "대건축 판당 평균 5.13개 / 7개 이상 0판"
- "AI 조선소 0채 / navy 도달 0판"
- 화면별 회랑 폭 표

**이 값들을 근거로 한 내 주장은 전부 근거를 잃는다.**
특히 대건축 임계를 5로 낮추자던 제안은 이미 다른 이유로 철회했는데, **이제 잰 값 자체도 무효다.**

---

## 하네스 처방 (3종 모두에 적용)

```js
// 페이지 로드 전
window.requestAnimationFrame = () => 0;      // ② 이중 스텝 차단
// 게임 시작 직전
muted = true;                                 // ① 사운드의 난수 절도 차단
// 양보 리듬은 반드시 고정 상수로              // ③ setTimeout 순서 고정
```

**검증 절차:** 동일 시드로 **실행 리듬을 일부러 바꿔가며** 3회 돌려 전 지표가 일치하는지 확인한다.
한 번만 돌려서 같은 값이 나오는 것으로는 부족하다 — 리듬이 같으면 우연히 일치한다.

이 검사를 통과하기 전의 A/B는 어느 것도 신뢰할 수 없다.
