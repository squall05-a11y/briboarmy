# 하네스 사용법

전제: node 설치 환경(Claude 세션 내 제공). `/tmp/v8.js`가 없으면 하네스가 `index.html`에서 자동 추출한다(수동 추출도 가능).

## 하네스 4종

- 스모크: `node harness23.js` — 12분 상한 6판(보통3·어려움3), 에러·시대 진행 확인
- 2인 동기: `node harness27_2p.js` — 락스텝 완전 일치 검사
- 봇 매트릭스: `BUILD=mass|hero|civ|struct|mixed D=easy|normal|hard|hell node harness30.js`
  출력: 빌드|난이도|결과|**결착 시각**|시대|E건설|교착초|E해군|전술전환|E핵기지|E항모
  (2026-07-29 수술: 지휘부 사망 즉시 정지 — 판 길이가 진짜 결착 시각)
- A/B: `./runAB.sh 파일 0|1 빌드들` (0=자기평가 OFF, 1=ON)
- 진단: `D=난이도 node diag.js` — 수동 P 관전, 12초 적건설·시대 로그

## 결정성 (TASK-02, 2026-07-29)

네 하네스 모두 공통 모듈 **`detkit.js`** 를 쓴다. 같은 시드는 언제나 같은 판을 재생한다.

- **시드 난수**: `SEED=숫자` 환경변수(기본 4242). `Math.random`이 시드 기반으로 교체되고
  호출 수가 `__rngN`에 계수된다. harness23은 판마다 `시드+판번호×1000`으로 재시드한다.
- **가상 시계·가상 타이머 큐**: `performance.now`가 벽시계가 아니라 시뮬 시각을 읽는다.
  `setTimeout`/`setInterval`은 큐에 쌓였다가 매 `update()` 직후 만기분만 순서대로 실행된다
  (등록 순서 보존, 콜백 중 신규 등록분은 다음 스텝). **이전 스텁(`()=>0`)에서는 타이머
  로직 전체가 사각지대였다 — 이제 폭격 요청·함락 연출 등 `setTimeout` 경로가 검증 가능하다.**
- **rAF 차단**: 게임의 자체 루프가 수동 스텝과 이중으로 도는 것을 막는다.
- **muted=true**: 게임 시작 직전 설정. 사운드의 난수 소비(기기 샘플레이트 의존)를 봉인한다.
  ⚠ muted 전후의 단일 시드 결과 비교로 밸런스 결론을 내지 마라 — 난수 스트림이 밀릴 뿐
  편향이 아니다. 시드를 바꾼 것과 같다.
- 각 하네스는 종료 시 `DET|…` 지표 라인을 출력한다: 판 길이·양측 킬·시대·골드·지휘부 HP·
  병력 수·`rng`(난수 호출 수)·`vtQ`(대기 타이머)·`vtErr`(타이머 콜백 오류 수).

## 결정성 검사 절차

```
node det-check.js              # 하네스 4종 전부
SEED=123 node det-check.js     # 다른 시드로
BUILD=civ D=hell node det-check.js   # harness30 조건 변경
```

동일 시드로 **실행 리듬(양보 간격·시간)을 일부러 바꿔가며** 3회 실행해 stdout 완전 일치와
난수 카운터 생존(0도 NaN도 아닌 정수)을 검사한다. **리듬을 고정한 3회 일치는 통과가 아니다.**
하네스 수정 후, A/B 측정 전에 반드시 이 검사를 통과시켜라.
`RHYTHM=스텝수:ms` 환경변수로 개별 하네스에 리듬을 직접 줄 수도 있다.

## 한계 3 (항상 명시)

① ~~setTimeout 스텁~~ → **가상 타이머로 해소** (렌더는 여전히 스텁 — 연출 의존 판정은 보정 판독)
② 봇 입력 — 사람 플레이 패턴 아님 (봇은 기념비를 지키지 않는다, TASK-01 실측)
③ UI 사각지대 — 터치·버튼 표시·iOS 오디오는 실기 스샷으로만 검증. CLAUDE.md 참조.
