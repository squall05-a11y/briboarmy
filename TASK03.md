# TASK-03 — 승급 버그 3종 + 경제 상한 (측정)

작성: 코워커 · 2026-07-29
선행: `CLAUDE.md`, `HARNESS.md`(TASK-02 갱신본), `BASELINE-DET-20260729.txt`

> **이 작업은 전부 "측정 승인"이다. 유지 여부는 사장 승인 사항이다(상시 규칙 3).**
> 각 단계는 **독립 커밋 · 독립 A/B · 독립 보고**다. 절대 묶지 마라.
> 우리는 방금 자를 고쳤다. **하나씩 재는 것이 이 작업의 목적이다.**

---

## 0단계 — 자 마무리 (선행, 필수)

**A/B를 시작하기 전에 두 가지가 남아 있다.**

### 0-1. `runAB.sh`가 `SEED`/`RHYTHM`을 전파하는지 검증

`det-check.js`의 `TARGETS`에 `runAB.sh`가 없다. **하네스 하나는 결정적인데 A/B는 아닐 수 있다.**
전파되지 않으면 전파하도록 고쳐라. **이게 안 되면 아래 전부가 무의미하다.**

### 0-2. `harness23`이 게임에 없는 기능을 쓴다

38줄 `useTactic(P)` — **판당 17~108회 호출된다.** 게임 본체는 0회다(로드맵 2번의 문제 그 자체).
41줄 `launchNukeAt(E,...)` — AI 핵도 대신 쏴준다. 게임에서 AI 핵은 현재 죽은 코드다.

**`harness30`은 정상이다** — 73줄에서 `P.rallyT=10; P.skillCd=40`으로 실제 버튼과 같은 경로를 쓴다.

→ **`harness23`을 `harness30`과 같은 경로로 맞춰라.** `useTactic(P)` 호출을 `P.rallyT=10; P.skillCd=40; P.skillCdMax=40;`으로 교체.
AI 핵 대행(41줄)은 **일단 그대로 두되 보고에 명시하라** — 제거하면 스모크의 성격이 바뀐다.

로드맵 2번(전술 재연결)이 끝나면 둘 다 `useTactic`으로 되돌린다. **주석으로 남겨라.**

---

## 1단계 — 승급 위치 선택을 거리 기반으로

### 문제

`checkPromote`(1099줄)가 합칠 5기를 **배열 순서로** 고른다.

```js
const same = army.filter(u=>u.side===side && u.id===id && u.tier===tier);
for(let i=0;i<5;i++){ const u=same[i]; px+=u.x/5; py+=u.y/5; ... }
```

**거리가 판정에 안 들어간다.** 실측(오퍼스): 승급 **520회/판**, 합병 5기의 y 산개폭 **최대 1,355px**(월드 높이 약 2,000px).
**전장 세로의 3분의 2에 걸쳐 흩어진 5기가 하나로 합쳐진다.** 적진의 병사와 본진의 신병이 무게중심에서 만난다.

### 지시

`same`에서 **서로 가까운 5기**를 고르도록 바꾼다.

**권고 구현(싸고 효과 대부분을 얻는다):** `same[0]`을 기준점으로 잡고 거리순 정렬 후 앞 5기.
전체 쌍 최소밀집을 찾는 건 비싸다. 하지 마라.

새 유닛 위치는 **그 5기의 무게중심 그대로** 둔다. 5기가 가까우면 무게중심도 가깝다.

### 측정

- **합병 5기의 y 산개폭** — 최대·평균 (수정 전 최대 1,355px)
- 승급 횟수 (수정 전 520회/판 — 크게 변하면 안 된다)
- 판 결과 5종: 판 길이 · 양측 킬 · 양측 시대 · 승패 · 병력 수

---

## 2단계 — 승급 시 `mode` 승계

### 문제

`army.push({side,id,tier:nt, hp, maxHp, cd:0, x,y,hx,hy, hitT:0})` — **`mode` 필드가 없다.**
실측: 승급 유닛 **18기 전부 `undefined`**.

`undefined`는 어느 분기에도 안 걸려서 **세 가지가 동시에 일어난다.**

**① 명령 소실** — 대기도 돌격도 아닌 상태

**② 돌격 속도 배수를 못 받음** — 1471줄 `spd = ROLE_SPD[…] * (mode==="attack" ? 2.6 : 1)`

코드상으로는 근접 98.8 → 38.0, 탱크 72.8 → 28.0이 되어야 한다.

**그런데 실측은 1.03배다** (오퍼스: 프레임당 전진 승급 2.045px vs 비승급 2.105px).

**이유: 2.6 배수는 "표적을 향해 이동하는 분기"에서만 붙는다.**
승급 유닛은 ①번 때문에 무게중심(대개 후방)에 생성되고, 거기서는 표적이 `engageR` 밖이라
그 분기에 들어가지 않는다. **대열 복귀 경로(`min(DEPLOY_SPD*0.5, ROLE_SPD)`)로 빠진다.**

→ **승급 유닛은 느리게 걷는 게 아니라 애초에 앞으로 안 간다.**
→ **①을 고치기 전에는 이 결함이 측정되지 않는다. 그래도 코드에는 실재한다.**

**③ 중앙선을 못 넘음** — 1475·1565줄 클램프가 `mode!=="attack"`이면 0.545를 적용한다.
**`Math.max`라서 뒤로 밀지 않고 앞을 막는다. 텔레포트가 아니라 벽이다.**
(오퍼스의 "프레임 이동량 60px 초과 0회"는 이것과 모순되지 않는다. 벽은 프레임 델타로 안 잡힌다.)

**④ 항공모함이 전투기를 안 띄운다** — 발진 조건이 `dC.carrier && u.mode==="attack"`이다.
승급한 항모는 소티를 영원히 안 띄운다.

### 지시

승급 유닛에 `mode`를 부여한다.

**권고: `side.stance === "attack" ? "attack" : "hold"`** — 스탠스가 진실의 원천이다.
5기의 `mode`가 서로 다를 수 있으므로 다수결보다 이쪽이 단순하고 예측 가능하다.

**`grace`(1.2초 무적)는 주지 마라.** 승급은 신병이 아니다. **다만 이 판단을 보고에 명시하라.**

### 측정

**"순간이동"도 "평균 속도"도 아니다. 둘 다 실측에서 반증됐다.**

| 지표 | 왜 이것인가 |
|---|---|
| **승급 vs 비승급의 최소 도달 y** | **주 지표.** 얼마나 앞까지 갔는가. 평균 속도는 대열 복귀 구간이 섞여 희석된다(1.03배로 나왔다) |
| **승급 유닛 중 중앙선(0.545·VH)을 넘은 비율** | 벽의 직접 지표. 수정 전 0%로 예측 |
| 승급 유닛이 "표적 추격 분기"에 들어간 비율 | ②의 배수가 실제로 적용될 기회가 있었는지 |
| 판 결과 5종 | |

**평균 전진 속도는 보조로만 기록하라.** 주 지표로 쓰면 또 "효과 없음"으로 오독한다.

---

## 3단계 — `mode` 판정 기준 통일

### 문제

**`mode`를 해석하는 곳 셋의 기준이 다르다.**

| mode | 1411 leash (`==="hold"`?) | 1475·1565 클램프 (`!=="attack"`?) | |
|---|---|---|---|
| attack | 0.045 개방 | 0.045 개방 | 일치 |
| hold | 0.545 중앙 | 0.545 중앙 | 일치 |
| **stand** | **0.045 개방** | **건너뜀** | ⚠ **정지 명령인데 전선 개방** |
| undefined | 0.045 개방 | 0.545 중앙 | ⚠ 반대 (2단계로 소멸) |

**`stand`는 2단계로 사라지지 않는다. 독립 문제다.**
853줄 `doStand`와 2834줄 스탠스 토글에서 실제로 설정되는 모드다.
평소엔 `engageR` 190이라 안 움직여 안 보이지만, **`_invaded`면 520이 되고 클램프가 없어서 계속 쫓아간다.**
"그 자리 정지" 명령인데 침공 시엔 520px까지 추격한다.

### 지시

**1411줄의 `u.mode==="hold"`를 `(u.mode==="hold" || u.mode==="stand")`로 바꾼다.**

**단 오퍼스 조건: 1단계 A/B 결과를 본 뒤에 착수 여부를 판단한다.** 1단계가 판을 크게 바꾸면 이 단계의 기준선이 달라진다.

### 측정

- `stand` 상태 유닛의 최소 도달 y (수정 전/후)
- `_invaded` 발생 판에서 `stand` 유닛의 추격 거리
- 판 결과 5종

---

## 4단계 — 경제 보너스 상한 제거 (측정 전용)

### 문제

1353줄 `bankB = Math.min(bankB, 2.5)`.

**이 상한은 시대 0~4에서 한 번도 작동하지 않는다.** 근세에 시장을 전부 지어야 2.44다.

| 시대 | 시장 만재 시 | 상한 적용 | 잘림 |
|---|---|---|---|
| 석기~근세 | 1.36 ~ 2.44 | 그대로 | **0%** |
| 산업 | 2.82 | 2.50 | 11% |
| 현대 | 3.24 | 2.50 | 23% |
| **미래** | **6.76** | 2.50 | **63%** |

**순수하게 후반 억제 장치다.** 미래 시대에는 시장 3번째부터 완전히 낭비다.
초당 골드: 미래 시대 **208 → 561 (2.7배)**.

**미래 시대 시장은 이미 `×1.8` 특수 배수를 갖고 있다**(1353줄 `(bt>=7)?1.8:1`). 상한을 풀면 이게 그대로 살아난다.

### 지시

**`Math.min(bankB, 2.5)`를 제거하고 측정한다. 유지 여부는 사장 승인 사항이다.**

### 측정 — **반드시 두 갈래로 나눠 보고하라**

| | 검증 대상 | 예측 |
|---|---|---|
| **(a)** | **양측 시대 도달률** — 시대 5·6·7 도달 판 수 | **오를 가능성 있다.** 돈이 실제 경로다(`lumpCost` 일시불 진급, 비용 붙는 civ 카드) |
| **(b)** | **후반 구조물 등장** — 핵기지·항모·아이언돔·요격·대공포 | **0으로 유지될 것으로 예측한다.** 원인이 `aiUtilityAct` 후보 부재이고, 돈이 없는 코드 경로를 만들지는 못한다 |

**(a)와 (b)를 한 지표로 묶지 마라.** (a)가 올라도 (b)가 0이면 "효과 없음"으로 오독한다.

**(b)가 예측대로 0이면 그것이 성과다.** `aiUtilityAct` 원인이 추정에서 **대조군을 가진 결론**으로 바뀐다.
**(b)가 0이 아니면 우리가 뭔가 놓친 것이다. 그것도 성과다.**

---

## 왜 하나씩인가 — **버그가 버그를 가린다**

이 프로젝트가 반복해 온 형태에 이름을 붙인다.

> **코드에 실재하는 결함이 다른 결함 때문에 발현되지 않는다.**
> 가리는 쪽을 먼저 고치면 가려졌던 쪽이 드러나고,
> **순서를 거꾸로 하면 "효과 없음"으로 오독하고 넘어간다.**

지금까지 전부 이 형태였다.

| 가려진 것 | 가린 것 |
|---|---|
| 진짜 판 길이 | 좀비 구간(`endGame` 미호출) |
| 조선소 부재 | 함선 누출 |
| 하네스 결정성 | 사운드 난수 절도 |
| **클램프 벽 · 2.6배 감속** | **승급 위치 소실 (①)** |

**마지막 줄이 이번 작업이다.** 클램프도 감속도 코드에 실재하는데 실측에서는 각각 0회·1.03배로 나온다.
**①이 둘 다 가리고 있기 때문이다.**

→ **1단계를 고친 뒤 2·3단계를 재측정해야 진실이 나온다.**
→ **묶어서 하면 무엇이 무엇을 푼 건지 영원히 모른다.**

## 공통 규칙

- **한 단계 = 한 커밋 = 한 A/B = 한 보고.** 취향이 아니라 필수다. 위 참조
- 각 단계 A/B는 **기준선과 같은 조건** — 시드 3종 × 5빌드 × 2난이도 = 30판
- **A/B 전에 `det-check.js`를 통과시켜라.** 코드를 고쳤으니 결정성이 유지되는지 다시 확인해야 한다
- **밸런스 수치를 바꾸지 마라.** 유닛 스탯·비용·시대 진급 시간
- 요청 밖 변경은 **먼저 문의**한다
- 보고는 한국어로, 변경 목록 전량, 각 블록 말미에 `[코드 → 사장]` 형식 대상 표기
- 산출은 **첨부**로. 푸시 시도 금지

## 이 작업이 답하지 못하는 것 (미리 명시)

이 넷으로 **후반 시대가 열리는지는 알 수 없다.** 좌표계도 `aiUtilityAct`도 손대지 않기 때문이다.
**이번 측정의 목적은 "싼 것부터 재서 큰 수술의 목표치를 정하는 것"이다.**
